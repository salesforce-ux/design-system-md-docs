How to update tokens from salesforce-design-system to docs-site

1. In salesforce-design-system, run `yarn build`
2. Copy generated file from `packages/design-tokens/dist/themes/cosmos/cosmos.global.tokens.flat.json` to `docs-site/`
3. Create new branch, push to remote.